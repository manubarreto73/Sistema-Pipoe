package com.pipoe.pipoeapi.dominio.solicitudes.entities;

/** Cómo se enteró del Modelo PipoE. OTRO se acompaña del texto libre `canalOtro`. */
public enum CanalDifusion {
    LIBRO,
    ARTICULO,
    SITIO_WEB,
    DOCENTE,
    AMISTAD,
    REDES_SOCIALES,
    OTRO
}
